const { resolve } = require('path');

module.exports = {
  config: resolve(__dirname, 'src', 'config', 'database.js'),
  'models-path': resolve(__dirname, 'src', 'models'),
  'migrations-path': resolve(__dirname, 'src', 'database', 'migrations'), // a pasta "migrations" é criada pelo sequelize
  'seeders-path': resolve(__dirname, 'src', 'database', 'seeds') // cria essa também
};
